import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => {
    return (
        <footer>
            <div>
                footer
            </div>
        </footer>
    );
};

export default Footer;